name: CD

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: macos-latest
    env:
      DEVELOPER_DIR: /Applications/Xcode_11.4.1.app/Contents/Developer
      SOLUTION_DIR: ./SByteDev.MvvmCross.Plugins.DateTimeConverter
      PROJECT_DIR: SByteDev.MvvmCross.Plugins.DateTimeConverter

    steps:
    - name: Checkout
      uses: actions/checkout@v2.0.0
    - name: Setup
      uses: actions/setup-dotnet@v1.4.0
    - name: Setup NuGet
      uses: NuGet/setup-nuget@v1.0.2
    - name: Restore NuGet packages
      working-directory: ${{ env.SOLUTION_DIR }}
      run: nuget restore
    - name: Build using MSBuild
      working-directory: ${{ env.SOLUTION_DIR }}
      run: msbuild -p:Configuration=Release
    - name: Push to NuGet
      working-directory: ${{ env.SOLUTION_DIR }}
      run: dotnet nuget push ${{ env.PROJECT_DIR }}/bin/Release/*.nupkg --source nuget.org --api-key ${{secrets.NUGET_API_KEY}} --skip-duplicate
